import './styles.css';
export const metadata={title:'Provider Ops',description:'Synthetic provider appointment and claims operations'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
